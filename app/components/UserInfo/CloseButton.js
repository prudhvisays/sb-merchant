import styled from 'styled-components';

const CloseButton = styled.div`
  position: absolute;
  margin-right: 0.3em;
  margin-top: 0.3em;
  right: 0;
  font-size: 1.8rem;
  cursor: pointer;
`;

export default CloseButton;
